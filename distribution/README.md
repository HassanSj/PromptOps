# Distribution output

The folder **`out/`** here is produced by:

```bash
npm run distribution
```

It is listed in `.gitignore` and is **not** committed. It contains only legal
metadata and short readme text—**no application source**—for optional public
sharing. See **`docs/GITHUB_AND_COPYRIGHT.md`**.
